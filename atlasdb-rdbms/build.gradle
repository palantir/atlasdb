apply from: "../gradle/shared.gradle"

dependencies {
  compile(project(":atlasdb-client"))
  compile 'org.jdbi:jdbi:2.59'
  compile 'com.h2database:h2:1.4.187'

  testCompile project(path: ":atlasdb-tests-shared", configuration: 'test')
}

