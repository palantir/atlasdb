// -*- groovy -*-
// METABUILD GENERATED metaconf/metaconf.groovy + metaconf/project/make.gradle.tmpl -> atlasdb/atlasdb-endpoint/make.gradle
apply from: "${rootProject.projectDir}/before.gradle"
sourceSets.main.java.srcDirs=[ file('src/main/java') ]
sourceSets.main.resources.srcDirs=[  ]
sourceSets.test.java.srcDirs=[  ]
sourceSets.test.resources.srcDirs=[  ]
sourceCompatibility = '1.8'
targetCompatibility = '1.8'
configurations.create('test').extendsFrom(configurations.testRuntime)
jar {
  baseName 'atlasdb-endpoint'
  exclude '.gitkeep'
  doFirst {
    manifest {
      from rootProject.ext.standardManifest
      def jarList = processJarListForManifest(sourceSets.main.compileClasspath)
      if (!jarList.isEmpty()) {
        attributes.put('Class-Path', jarList.join(' '))
      }
    }
  }
  doLast {
    rootProject.ext.pgdevArtifactNames.add(archiveName)
  }
}
task testJar(type: Jar) {
  baseName 'atlasdb-endpoint-test'
  from sourceSets.test.output
  exclude '.gitkeep'
  ext.is_test = true
  doFirst {
    manifest {
      from rootProject.ext.standardManifest
      def jarList = processJarListForManifest(sourceSets.test.compileClasspath)
      if (!jarList.isEmpty()) {
        attributes.put('Class-Path', jarList.join(' '))
      }
    }
  }
  doLast {
    rootProject.ext.pgdevArtifactNames.add(archiveName)
  }
}
assemble.dependsOn testJar
task mainSourcesZip(type: Zip) {
  baseName 'atlasdb-endpoint'
  from sourceSets.main.allSource
}
assemble.dependsOn mainSourcesZip
task testSourcesZip(type: Zip) {
  baseName 'atlasdb-endpoint-test'
  from sourceSets.test.allSource
}
assemble.dependsOn testSourcesZip
dependencies {
  test sourceSets.test.output
  compile(group: 'com.netflix.feign', name: 'feign-jackson', version: '8.6.1', configuration: 'default')
  {
    exclude(
      module: 'jackson-databind',
    )
  }
  compile(group: 'com.netflix.feign', name: 'feign-jaxrs', version: '8.6.1', configuration: 'default')
  compile(group: 'com.fasterxml.jackson.core', name: 'jackson-core', version: '2.5.3', configuration: 'default')
  compile(group: 'com.fasterxml.jackson.core', name: 'jackson-databind', version: '2.5.3', configuration: 'default')
  compile(group: 'io.dropwizard', name: 'dropwizard-core', version: '0.8.2', configuration: 'default')
  {
    exclude(
      group: 'joda-time',
      module: 'joda-time',
    )
    exclude(
      module: 'jackson-core',
    )
    exclude(
      module: 'jackson-databind',
    )
    exclude(
      module: 'jackson-datatype-guava',
    )
  }
  testCompile(group: 'junit', name: 'junit', version: '4.11', configuration: 'default')
  testCompile(group: 'io.dropwizard', name: 'dropwizard-testing', version: '0.8.2', configuration: 'default')
  {
    exclude(
      module: 'junit',
    )
    exclude(
      group: 'joda-time',
      module: 'joda-time',
    )
    exclude(
      module: 'jackson-core',
    )
    exclude(
      module: 'jackson-databind',
    )
    exclude(
      module: 'jackson-datatype-guava',
    )
  }
  compile(group: 'com.fasterxml.jackson.datatype', name: 'jackson-datatype-guava', version: '2.5.3', configuration: 'default')
  {
    exclude(
      module: 'guava',
    )
  }
  compile(group: 'joda-time', name: 'joda-time', version: '2.9.2', configuration: 'default')
  testCompile(project(path: ':atlasdb-impl-shared', configuration: 'test'))
  compile(project(path: ':atlasdb-impl-shared', configuration: 'default'))
  testCompile(project(path: ':atlasdb-config', configuration: 'test'))
  compile(project(path: ':atlasdb-config', configuration: 'default'))
}
configurations.all {
  resolutionStrategy {
    force 'com.google.code.findbugs:jsr305:1.3.9'
    force 'org.slf4j:slf4j-api:1.7.5'
    force 'org.apache.commons:commons-lang3:3.1'
  }
}
apply from: "${rootProject.projectDir}/after.gradle"
