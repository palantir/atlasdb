apply from: "../gradle/shared.gradle"

dependencies {
    api('com.palantir.cassandra:cassandra-all:' + libVersions.palantir_cassandra_thrift) {
        transitive = false
        exclude group: 'org.apache.httpcomponents'
    }
    api(group: 'org.apache.commons', name: 'commons-lang3', version: libVersions.commons_lang3)
    api(group: 'com.google.guava', name: 'guava', version: libVersions.guava)
}
