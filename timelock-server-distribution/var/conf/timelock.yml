clients:
  - test
  - test2
  - atlasete

cluster:
  localServer: localhost:8421
  servers:
    - localhost:8421

algorithm:
  type: paxos

server:
  applicationConnectors:
    - type: http
      port: 8421
  adminConnectors:
    - type: http
      port: 8422
