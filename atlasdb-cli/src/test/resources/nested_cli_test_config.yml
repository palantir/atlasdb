config:
  dropwizardConfig:
    server:
      applicationConnectors:
        - type: http
          port: 3828
      adminConnectors:
        - type: http
          port: 3829

    atlasdb:
      notAnAtlas: configuration

    real:
      atlasdb:
        namespace: memory
        keyValueService:
          type: memory

otherConfig:
  - type: otherType
