atlasdb:
  keyValueService:
    type: relational
    ddl:
      type: postgres
    connection:
      type: postgres
      host: postgres.palantir.pt
      port: 1000
      dbName: test
      dbLogin: test
      dbPassword: testpassword
      connectionTimeoutSeconds: 5
      socketTimeoutSeconds: 30
      connectionParameters:
        foo: 100
        bar: baz
      ssl: true
