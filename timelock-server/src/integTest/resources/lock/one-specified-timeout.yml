clients:
  - test
  - test2

cluster:
  localServer: localhost:8421
  servers:
    - localhost:8421

algorithm:
  type: paxos

server:
  applicationConnectors:
    - type: http
      port: 8080
      idleTimeout: 1 day
