algorithm:
  type: paxos
  paxosDataDir: <TEMP_DATA_DIR>

cluster:
  localServer: localhost:8080
  servers:
    - localhost:8080

clients:
  - test
  - test2
  - test3
  - learner
  - acceptor

server:
  minThreads: 1
  maxThreads: 50
