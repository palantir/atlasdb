jepsen:
  image: tjake/jepsen:latest
  volumes:
    - ./:/jepsen/atlasdb
    - /var/run/docker.sock:/var/run/docker.sock
  command: "bash -lc 'cd /jepsen/atlasdb && chmod +x ./src/jepsen/atlasdb/install_zulu.sh && ./src/jepsen/atlasdb/install_zulu.sh && lein test'"
