type: fix
fix:
  description: |-
    See PDS-293157.
    Presently, as sweep progresses on each host, the lastSweptTimestamp metric is updated lazily per shard. This PR makes it so we update that metric periodically for all shards by reading from the persisted shard progress table.
  links:
  - https://github.com/palantir/atlasdb/pull/6232
