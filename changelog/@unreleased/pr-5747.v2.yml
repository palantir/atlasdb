type: improvement
improvement:
  description: We now maintain a history of requests that need fresh timestamps from
    TimeLock server for additional logging if timestamp violations are observed.
  links:
  - https://github.com/palantir/atlasdb/pull/5747
