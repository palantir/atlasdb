type: improvement
improvement:
  description: |
    The retained memory in delete executor work queue is reduced. Previously, each runnable in deleteExecutor had SnapshotTransaction reference, which can be potentially huge.
  links:
  - https://github.com/palantir/atlasdb/pull/4656
