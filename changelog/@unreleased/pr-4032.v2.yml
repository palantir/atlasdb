type: improvement
improvement:
  description:  A new metric `futureValueCellFilterCount` is added to track number of cells filtered on client side after retrieved from C* as a result of having a timestamp bigger than start timestamp of the transaction.
  links:
  - https://github.com/palantir/atlasdb/pull/4032
