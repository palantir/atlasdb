type: improvement
improvement:
  description: Remove the H2 runtime dependency. Consumers who use H2 will need to
    explicitly depend on H2 themselves.
  links:
  - https://github.com/palantir/atlasdb/pull/6098
