type: improvement
improvement:
  description: Remove `ThrowingCqlClientImpl`, CKVS `getAsync` delegates to `get`
    instead of throwing an exception when using default or wrong CQL capable configuration.
  links:
  - https://github.com/palantir/atlasdb/pull/4461
