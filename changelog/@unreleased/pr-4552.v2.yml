type: improvement
improvement:
  description: TimeLock now logs ERROR messages if multiple nodes are detected to
    be concurrently servicing timestamps for a given namespace.
  links:
  - https://github.com/palantir/atlasdb/pull/4552
