type: fix
fix:
  description: '`TransactionManagers` now creates the `ClearsTable` during initialisation
    regardless of whether or not Targeted Sweep is enabled.'
  links:
  - https://github.com/palantir/atlasdb/pull/4164
