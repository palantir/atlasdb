type: improvement
improvement:
  description: |-
    New Exception classes:
    - CassandraTExceptions()
    - CassandraTimedOutException()

    Amended how UnavailableException Thrift Cassandra native Exceptions are caught, and they now get mapped to InsufficientConsistencyException() via CassandraTExceptions().
  links:
  - https://github.com/palantir/atlasdb/pull/7353
