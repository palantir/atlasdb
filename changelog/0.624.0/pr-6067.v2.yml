type: improvement
improvement:
  description: The current list of Timelock servers is now logged at `WARN` level
    when an `UnknownRemoteException` is encountered (rate limited to once per 20 seconds).
  links:
  - https://github.com/palantir/atlasdb/pull/6067
