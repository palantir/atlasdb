type: improvement
improvement:
  description: We now persist the cells to delete to the Scrub queue incrementally.
    This reduces memory pressure on user services.
  links:
  - https://github.com/palantir/atlasdb/pull/5509
