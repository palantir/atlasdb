type: feature
feature:
  description: The TimeLock corruption detecting checks are now enabled. As of now,
    all the violations are configures to only trigger alerts and not make TimeLock
    shoot itself.
  links:
  - https://github.com/palantir/atlasdb/pull/5090
