type: improvement
improvement:
  description: 'Atlas CLI now uses two transactions per getRange, instead of two transactions
    for the entire validation (note: one transaction is nested in the other).'
  links:
  - https://github.com/palantir/atlasdb/pull/4681
