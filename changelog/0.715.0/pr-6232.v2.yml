type: fix
fix:
  description: |-
    The `lastSweptTimestamp` metric for targeted sweep is now updated at regular intervals by reading persisted sweep progress. Previously, this was done lazily, which could result in stale metrics during periods with few writes.
  links:
  - https://github.com/palantir/atlasdb/pull/6232
