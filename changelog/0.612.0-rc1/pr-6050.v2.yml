type: fix
fix:
  description: '`Servers` and `replicationFactor` are now provided via a runtime config.
    `CassandraReloadableKeyValueServiceRuntimeConfig` is used to merge existing install
    and runtime config for Cassandra KVS constructed by a `CassandraAtlasDbFactory`'
  links:
  - https://github.com/palantir/atlasdb/pull/6050
