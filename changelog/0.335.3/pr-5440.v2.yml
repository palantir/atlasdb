type: improvement
improvement:
  description: Read-write transactions which have performed no writes will now get a commit timestamp if they have loaded values to the lock watch value cache.
  links:
  - https://github.com/palantir/atlasdb/pull/5440
