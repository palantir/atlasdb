types:
  definitions:
    default-package: com.palantir.timelock.feedback
    objects:
      ConjureTimeLockClientFeedback:
        fields:
          atlasVersion: string
          serviceName: string
          nodeId: uuid
          stats: map<string, EndpointStatistics>
      EndpointStatistics:
        fields:
          p99: double
          oneMin: double

services:
  TimeLockClientFeedbackService:
    name: TimeLock Client Feedback Service
    default-auth: header
    package: com.palantir.atlasdb.timelock.adjudicate.feedback
    base-path: /tl/feedback
    endpoints:
      reportFeedback:
        http: POST /reportFeedback
        args:
          feedback: ConjureTimeLockClientFeedback
        docs: |
          The endpoint recieves feedback reports from clients which are required for TimeLock Adjudication.
