version: '2'

services:
  postgres:
    image: kiasaki/alpine-postgres:9.6
    environment:
       POSTGRES_PASSWORD: palantir
       POSTGRES_USER: palantir
       POSTGRES_DB: atlas
    ports:
      - "5432"
