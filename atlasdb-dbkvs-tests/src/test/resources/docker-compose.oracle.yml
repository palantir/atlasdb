version: '2'

services:
  oracle:
    image: palantirtechnologies/oracle-atlasdb:19.20.0.0
    volumes:
      - ./docker-startup:/opt/oracle/scripts/startup
    ports:
      - "1521"
    environment:
      ORACLE_SYS_PASSWORD: palpal
      MEMORY: 2Gi
